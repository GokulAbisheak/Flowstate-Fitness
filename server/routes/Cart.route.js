// import express from 'express';
// import CartController from '../controllers/Cart.controller.js';

// const CartRouter = express.Router();

// // Get all items in cart for a specific user
// CartRouter.get('/:email', CartController.getAllItems);

// // Add an item to the cart for a specific user
// CartRouter.post('/add', CartController.createCartItem);

// // Remove an item from the cart for a specific user
// CartRouter.delete('/delete/:email/:id', CartController.removeItem);

// // Update the quantity of an item in the cart for a specific user
// CartRouter.put('/update/:email/:id', CartController.updateItemQuantity);

// // Empty the cart for a specific user
// CartRouter.delete('/:email', CartController.emptyCart);

// export default CartRouter;